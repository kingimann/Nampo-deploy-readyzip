/**
 * DocumentPicker — internal seam over the file picker.
 * Pass-through to `expo-document-picker` today; swap here later. Part of the
 * gradual move off Expo (see ./README.md).
 */
export * from "expo-document-picker";
