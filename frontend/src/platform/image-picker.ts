/**
 * ImagePicker — internal seam over the photo/video picker.
 * Pass-through to `expo-image-picker` today; the eventual bare-RN swap
 * (react-native-image-picker) happens here. Part of the gradual move off Expo.
 */
export * from "expo-image-picker";
