/**
 * Video — internal seam over the video view + player.
 * Pass-through to `expo-video` today; the eventual bare-RN swap
 * (react-native-video) happens here. Part of the gradual move off Expo.
 */
export * from "expo-video";
