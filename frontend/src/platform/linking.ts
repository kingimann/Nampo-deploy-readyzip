/**
 * Linking — internal seam over deep-linking / URL handling.
 * Pass-through to `expo-linking` today; swap here later. Part of the gradual
 * move off Expo (see ./README.md).
 */
export * from "expo-linking";
