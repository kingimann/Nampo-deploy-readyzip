/**
 * Speech — internal seam over text-to-speech.
 * Pass-through to `expo-speech` today; the eventual bare-RN swap (e.g.
 * react-native-tts) happens here. Part of the gradual move off Expo.
 */
export * from "expo-speech";
