/**
 * Audio — internal seam over audio playback.
 * Pass-through to `expo-audio` today; the eventual bare-RN swap (e.g.
 * react-native-track-player / react-native-video) happens here. Part of the
 * gradual move off Expo.
 */
export * from "expo-audio";
