/**
 * LinearGradient — internal seam over the gradient component.
 * Pass-through to `expo-linear-gradient` today; the eventual bare-RN swap
 * (react-native-linear-gradient) happens here. Part of the gradual move off Expo.
 */
export { LinearGradient } from "expo-linear-gradient";
