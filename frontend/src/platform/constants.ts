/**
 * Constants — internal seam over app/runtime constants.
 * Pass-through to `expo-constants` today; swap here later. Part of the gradual
 * move off Expo (see ./README.md).
 */
export * from "expo-constants";
export { default } from "expo-constants";
