/**
 * Camera — internal seam over the camera view + permissions.
 * Pass-through to `expo-camera` today; the eventual bare-RN swap
 * (react-native-vision-camera) happens here. Part of the gradual move off Expo.
 */
export * from "expo-camera";
