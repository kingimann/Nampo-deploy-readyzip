# Expo SDK 54 + some config plugins (e.g. @config-plugins/react-native-webrtc)
# still declare an older `expo` peer range. npm's strict peer resolution would
# reject that; yarn only warns. Resolve peers leniently (matching yarn) so
# `npm ci` / `npm install` succeed in CI and on Render.
legacy-peer-deps=true
